# Translation Popup sessions

The selection translation UI is a frameless, always-on-top Slint card managed by `lexift-ui`. Windows applies `WS_EX_NOACTIVATE | WS_EX_TOOLWINDOW` before showing the card, then removes `WS_EX_NOACTIVATE` with an `SWP_NOACTIVATE` frame refresh immediately after the show completes. This keeps focus in the source application during passive display.

Slint creates the native HWND for a newly allocated top-level window on a later Winit event-loop turn. Passive preparation therefore reports `Pending` while the handle is unavailable. `PopupRegistry` retains the latest show request and retries after yielding to the event loop, then at 16 ms intervals for up to one second. Hiding or closing the session invalidates the pending generation. A temporary missing handle is not logged as a platform failure and never requires a second hotkey press.

Each Popup HWND installs one replaceable Win32 pointer bridge. It consumes mouse movement, leave, left-button, capture, and wheel messages before Winit and forwards them with `slint::Window::dispatch_event_with_result`. Client coordinates remain physical in `lexift-platform`; `lexift-ui` divides them by the live Slint scale factor, including wheel deltas, before dispatch. The first press uses a temporary `AttachThreadInput` connection to activate and focus the Popup, sends a position update before the press, and captures the mouse until release.

The HWND property `Lexift.PopupInputBridge` owns the bridge pointer and identifies an existing installation without relying on the unavailable `GetWindowSubclass` export. Reused HWNDs replace only the stored handler and reset transient pointer state. `WM_NCDESTROY` removes the property and subclass before releasing the bridge.

An unpinned window remains visible for passive reading, then closes on the next pointer press outside its bounds or on a subsequent foreground-window change. The initial foreground window is recorded when the popup is shown so passive display never dismisses it immediately. Pinning unregisters this monitor; pinned windows remain visible and always on top.

## Core ownership

`lexift-core` owns `PopupSessionId` and `PopupSessionState`. A session contains source and translated text, its user-selected source language, per-window target language, provider-detected source language, translation task, pin state, feedback, and speech state. `None` for the selected source language means automatic detection. The active unpinned session is reused by later selections and resets its source choice to automatic detection for every new capture. Pinning detaches it from the active slot, so the next selection creates another session while the pinned session keeps its source choice. Task IDs are checked per session, and results for superseded or closed sessions are ignored.

Changing either popup language only changes that session and immediately starts a new translation with the complete source/target context. It does not write Settings. An explicit source choice is preferred for source speech; automatic mode uses the provider-detected language. Copy and speech requests are explicit Core commands executed by adapters.

## UI and native lifecycle

`PopupRegistry` runs on the Slint event-loop thread and maps session IDs to `TranslationPopup` windows. It creates windows only for live sessions and destroys each component after that session closes, including its pointer bridge, dismissal watch, pending show, drag, resize, and manual-size state. Fixed sessions keep independent windows until explicitly closed. New windows still use the pending-HWND retry path, so the first `Alt+X` displays the Popup. The registry applies Core snapshots, positions new windows by the selection anchor, offsets them from visible pinned windows, and clamps resized windows to the monitor work area. Each window attaches its own session ID to callbacks.

The card keeps source, language, and result sections visible. Source and result content grow within capped regions and scroll independently; the editable source area follows the text cursor and shows its vertical scrollbar only when needed. The compact language row reserves a fixed center swap area and divides the remaining width equally between source and target selectors. Provider detection remains session state for speech selection but is not rendered as a separate label.

The Popup's rounded opaque shell fills the native window bounds, so the interface reads as one window instead of a card inset on a white surround. Windows uses one outer-corner source: successful DWM setup lets the system clip the opaque surface; unavailable DWM rounding keeps a square opaque shell; other platforms use the Slint rounded shell. Corner resize cues follow the active corner mode. The title row is draggable below the top-edge resize strip, while the Pin and Close buttons remain interactive. The border provides eight resize handles, with directional cursors and short low-emphasis edge accents. A handle only registers platform-side resize tracking; it starts no timer and schedules no paint. The center of the top 8-DIP strip uses Win32 `HTTOP` so DefWindowProc enters the non-client sizing loop directly, which is what lets Windows own its vertical-maximize preview. Other handles use upstream Winit's `drag_resize_window` with the matching `ResizeDirection`. Windows then owns the whole sizing loop and keeps the opposite edge anchored; the adapter never writes geometry during it. Slint's own window constraints (`min-width`, `min-height: resize-min-height`, `max-popup-width`, `max-popup-height`) travel through Winit into `WM_GETMINMAXINFO`, so size limits need no adapter code. During the loop the Slint layout follows the native window size at whatever rate the backend paints, and no code throttles or coalesces those paints; a slow drag may therefore show the window background where content has not caught up yet. Slint asks winit for a transparent window on every platform, and on Windows winit implements that with `DwmEnableBlurBehindWindow` and an empty blur region, which switches the window to per-pixel alpha: every pixel the application has not painted, including a surface the compositor recreated after the window left the screen, would show the desktop straight through. Because the Popup and Settings are opaque panels, the adapter disables that per-pixel alpha again when it prepares them. Winit also leaves the window class without a background brush and the software renderer presents only its own damage rectangles, so nothing would paint the band a resize exposes; `configure_resize_background` installs a per-window subclass that fills exactly that band, and the update region Windows erases, with `resize-fallback-color` (`Colors.surface` for the Popup, `Colors.background` for Settings). The band is covered twice: once when the size message arrives, and again inside the paint cycle before the application presents. The second pass matters because the application calls `ValidateRect` for the whole window when it presents, so a band that only the size-time pass painted can be lost with the old compositor surface and would then keep stale pixels until something else changes the window. Newly exposed client area therefore shows the window's own colour instead of black or another window's stale pixels. While the handle that changes height is active, the source card follows the window height through `resize-changes-height`/`resize-fixed-remainder`; the rest of the card layout is the same as any other size change. That follow height is floored at `resize-min-source-height` and capped by `resize-max-source-height` (`window height - resize-layout-reserve-height`), so shrinking a tall Popup lets the result card absorb the change instead of collapsing the source card to nothing, and the card can never grow past the space the language row and the result card need. The cap has to live in the Slint expression because Windows shrinks the window itself in two cases the UI cannot observe in time: dragging the title row out of a top-edge snap restores the pre-snap rectangle, and a cross-DPI or cross-monitor move resizes the window, while both run inside a modal move loop that blocks the Slint event loop. The bridge therefore only re-clamps the saved card height after the mouse is released, which would otherwise leave the source card filling the whole window and push the rows below it out of view for the length of the drag. Popup size limits keep two invariants: the minimum height never exceeds the maximum, and the minimum never depends on the window's current height. It is the layout floor instead (`layout reserve + minimum source card`, 240 + 86 here, raised to the 336-DIP design floor), and every clamp goes through an ordering-safe helper because the release profile uses `panic = "abort"` and `f32::clamp` panics when `min > max`, so an inverted range must never reach the sizing loop. Deriving the floor from the live height (`window height - source card + minimum source card`) was wrong: a Popup with a short source text keeps its source card at the 86px minimum, so the derived minimum equalled the current height. After a vertical snap to the monitor work area that height was also the monitor maximum, which left Winit `WM_GETMINMAXINFO` bounds of exactly one value, and Windows then refused every height change: the four corners and the top edge looked dead while only horizontal drags still worked. The adapter only fills the bands a size change uncovers, reusing one brush per window, and it deliberately does not handle `WM_ERASEBKGND`. Two earlier mechanisms were removed on 2026-09-25 and the reason matters for both. A frame copy that pasted the last presented frame back after a move is gone: both windows are opaque DWM-composited panels and nothing overwrites their pixels any more, so there is nothing to repair (see `devdoc/resize-performance.md` for the corrected A/B). The erase fill is gone too, and that one was causing a visible bug rather than just costing time: moving a window off the screen and back makes Windows invalidate the part that returns, so erasing it painted the flat window background over content the software renderer never repaints, leaving a blank band in the window colour until something else repainted it. `WM_ERASEBKGND` is now left to the default handler, and the window class has no background brush, so a move cannot destroy pixels. `WM_EXITSIZEMOVE` ends the session: the adapter always reports the final client size once, even for a press that never moved the window, because the UI owns the matching resize state and only clears it from that report; `WM_CANCELMODE`, hiding, and destruction clear the rest. The UI then applies the exact final size, refreshes the monitor work area from the Popup's current window center for cross-monitor clamps, and stores the manual size for the session. The adapter does not draw a preview and does not apply a full-height rectangle on release. Other resize directions and top-edge drags outside the trigger area retain Windows' normal anchored sizing behavior. The `WS_EX_TOOLWINDOW` style remains in place so the Popup stays out of the taskbar and Alt+Tab. After vertical resizing, the source editor grows only while the layout budget can still preserve the 44px language row and 104px minimum result card; any remaining height expands the result card. The same bound applies to the saved manual size and a reopened Popup, while shrinking keeps the source editor at least 86px high. Each session also stores the automatic-content baseline, so later text growth may extend its manual size while shorter content never reduces it. The record is discarded when the session closes or its window slot is reused. Interactive resizing writes no per-resize log line.

Both language selectors share one `PopupLanguageMenuWindow` instance at a time. It is created only when a selector opens and destroyed when the menu closes. The menu records whether its owner is the source or target selector, aligns to that control, shows up to ten rows, flips above the selector or reduces its scrollable height when the monitor work area is constrained, and never changes the translation card's size. Opening one side closes the other. Source options are automatic detection plus the reduced set accepted by DeepL as explicit source languages; target options retain their regional variants. Popup and Settings menus share the same menu typography tokens and snap scroll offsets to stable coordinates to keep Windows text rendering crisp across DPI scales.

While the language menu is open, dismissal monitoring moves from the parent Popup to the menu HWND. A click inside the menu selects a language for the owning session; a click elsewhere closes only the menu and then restores the parent's unpinned dismissal watch. Dragging, hiding, closing, or reusing the owning Popup also closes the menu and clears its scroll and pending native-window state. The menu uses the same delayed HWND preparation, native pointer bridge, DPI conversion, and tool-window ownership as translation windows.

Windows supplies passive/interactive style changes, native custom-title-bar dragging, outside-interaction monitoring, and a dedicated STA SAPI worker. The worker selects a matching installed voice by language when available, falls back to the system voice, replaces the current utterance on a new request, and reports completion or failure back to Core.

Popup sessions are intentionally in-memory only and end when their windows close or the process exits.

The Main and Settings components are also created on demand. Background startup creates no Slint window components. Closing Main with a working tray destroys the component while leaving the process, tray, global hotkey, and Slint event loop running; a tray or second-instance open request reconstructs Main from the latest Core snapshot. Closing or hiding Settings clears credential drafts and transient feedback before destroying it; opening Settings again reconstructs it from current state. Closing without a usable tray retains the existing exit behavior. Destroying the final window releases that window's Skia renderer, surface, and GPU resources; the process-level Slint/Winit event loop and allocator remain alive. After two minutes with no window components, the Windows adapter asks the OS to trim the process working set. This evicts resident pages but does not decommit private allocations or recreate the renderer backend, and pages may fault back in on the next UI request.
